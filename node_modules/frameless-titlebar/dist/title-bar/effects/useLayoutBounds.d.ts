import { RectResult } from '../typings';
declare const useLayoutBounds: (bounds: RectResult, depth: number, hasSubLabel: boolean) => {
    top: string;
    left: string;
};
export default useLayoutBounds;
