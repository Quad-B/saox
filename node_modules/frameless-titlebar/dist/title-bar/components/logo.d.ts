import React from 'react';
interface LogoProps {
    children: React.ReactNode;
    src?: string;
    hasTitle: boolean;
}
declare const Logo: ({ children, src, hasTitle }: LogoProps) => JSX.Element;
export default Logo;
