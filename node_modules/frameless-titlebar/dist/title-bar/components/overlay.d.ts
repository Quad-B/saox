import React from 'react';
interface OverlayProps {
    top: number | string;
    onClick?: (e: React.MouseEvent) => void;
}
declare const Overlay: ({ top, onClick }: OverlayProps) => JSX.Element;
export default Overlay;
