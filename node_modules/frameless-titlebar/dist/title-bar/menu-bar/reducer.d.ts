export declare type MenuState = {
    selectedPath: number[];
    altKey: boolean;
};
export declare type MenuAction = {
    type: 'alt';
    altKey: boolean;
} | {
    type: 'reset';
} | {
    type: 'del';
    depth: number;
} | {
    type: 'set' | 'hover-sub' | 'button-set';
    depth: number;
    selected: number;
};
export declare const initialState: MenuState;
declare const menuReducer: (state: MenuState, action: MenuAction) => MenuState;
export default menuReducer;
