/// <reference types="react" />
import { MenuItemProps } from '../typings';
declare const MenuItemComponent: ({ item, onClick, currentWindow, depth, dispatch, selectedPath, idx }: MenuItemProps) => JSX.Element | null;
export default MenuItemComponent;
