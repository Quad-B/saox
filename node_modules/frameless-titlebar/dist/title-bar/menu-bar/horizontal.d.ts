/// <reference types="react" />
import { HorizontalMenuProps } from '../typings';
declare const HorizontalMenu: ({ menu, focused, currentWindow, menuBar }: HorizontalMenuProps) => JSX.Element;
export default HorizontalMenu;
