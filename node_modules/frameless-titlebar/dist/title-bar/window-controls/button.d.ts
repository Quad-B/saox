/// <reference types="react" />
import { WindowButtonProps } from '../typings';
declare const WindowButton: ({ children, onClick, close, controls, platform }: WindowButtonProps) => JSX.Element;
export default WindowButton;
