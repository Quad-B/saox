/// <reference types="react" />
import { TitleBarProps } from './typings';
declare const TitleBar: ({ onMinimize, onMaximize, onClose, onDoubleClick, disableMaximize, disableMinimize, platform, children, theme, menu, icon, iconSrc, title, currentWindow }: TitleBarProps) => JSX.Element;
export default TitleBar;
